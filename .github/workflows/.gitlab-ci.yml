stages: # Define el orden de las etapas
  - build
  - test

image: node:latest # Imagen Docker por defecto

build_job:
  stage: build
  script:
    - npm install
    - npm run build

test_job:
  stage: test
  script:
    - npm test
